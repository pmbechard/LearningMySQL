USE sql_store;

/*
 NOTE: Click Modify Column to see more details in DataGrip

 Datatypes:
 INT(11)            type(max spaces)
 VARCHAR(50)
    VARCHAR means the input can be <= max
    In contrast, CHAR(50) would input an extra 45 spaces if a 5-letter value input
 DATE

 PK = Primary Key (symbolized by golden key)
 NN = Not NULL; means every entry must have a value for that column
        (symbolized by circle in bottom-left corner)
 AI = auto increment; every new record is automatically given an incremented value
        very useful for primary keys and other unique ids
 Default = given value if none is provided
 Unique = must be different from other entries' data
 */